import '../styles/quote.css';

const Quote = () => {
    return (
        <div className="parallax-bg3">
            <blockquote>
                <p>I adhere to two adages, 'Nothing is impossible or difficult', 'There are only solutions'.</p>
            </blockquote>
        </div>
    );
};

export default Quote;
